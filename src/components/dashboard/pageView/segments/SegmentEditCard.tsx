"use client";

import { ExtendedSegment } from "@/lib/types/pageTypes";

const SegmentEditCard = ({
	segment,
}: Readonly<{ segment: ExtendedSegment }>) => {
	return <></>;
};

export default SegmentEditCard;
